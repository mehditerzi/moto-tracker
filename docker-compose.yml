services:
  api:
    build:
      context: .
      dockerfile: apps/api/Dockerfile
    container_name: mototracker-api
    restart: unless-stopped
    environment:
      NODE_ENV: production
      PORT: 8787
      DATABASE_PATH: /data/app.db
      UPLOADS_DIR: /data/uploads
      WEB_ROOT: /app/public
      TRUST_PROXY: "true"
      # Public origin where the app is reachable through ngrok.
      APP_BASE_URL: ${APP_BASE_URL}
      SESSION_SECRET: ${SESSION_SECRET}
      # iOS (Capacitor) wrapper support — leave both unset for the web deploy.
      # When shipping the native app, set in .env:
      #   WEB_ORIGIN=capacitor://localhost   (adds it to CORS + auth trusted origins)
      #   CROSS_SITE_COOKIES=true             (session cookie SameSite=None; Secure)
      WEB_ORIGIN: ${WEB_ORIGIN:-}
      CROSS_SITE_COOKIES: ${CROSS_SITE_COOKIES:-false}
      # CSP ships report-only until it has been verified on a real device.
      # The iOS wrapper loads this origin through Capacitor's server.url, so an
      # over-strict script-src could disable StoreKit / push / geolocation on
      # every installed phone at once. Flip to "false" to enforce once the
      # device check is clean — see docs/operations.md.
      CSP_REPORT_ONLY: ${CSP_REPORT_ONLY:-true}
      RESEND_API_KEY: ${RESEND_API_KEY:-}
      EMAIL_FROM: ${EMAIL_FROM:-Garajım <noreply@example.com>}
      # Local SMTP fallback (used only when RESEND_API_KEY is empty). To catch
      # mail in the bundled Mailpit container, set SMTP_HOST=mailpit in .env and
      # `docker compose --profile mail up -d`. Inbox UI → http://localhost:8025.
      SMTP_HOST: ${SMTP_HOST:-}
      SMTP_PORT: ${SMTP_PORT:-1025}
      SMTP_SECURE: ${SMTP_SECURE:-false}
      SMTP_USER: ${SMTP_USER:-}
      SMTP_PASS: ${SMTP_PASS:-}
      GOOGLE_CLIENT_ID: ${GOOGLE_CLIENT_ID:-}
      GOOGLE_CLIENT_SECRET: ${GOOGLE_CLIENT_SECRET:-}
      # Sign in with Apple (Service ID + ES256 client-secret JWT; see
      # docs/apple-signin.md and scripts/apple-client-secret.mjs).
      APPLE_CLIENT_ID: ${APPLE_CLIENT_ID:-}
      APPLE_CLIENT_SECRET: ${APPLE_CLIENT_SECRET:-}
      # By default the api talks to an Ollama already running on the host
      # (Docker Desktop / Linux with host-gateway). To run a bundled Ollama
      # container instead, set OLLAMA_URL=http://ollama:11434 in .env and
      # `docker compose --profile bundled-ollama up -d`.
      OLLAMA_URL: ${OLLAMA_URL:-http://host.docker.internal:11434}
      # OCR model defaults — measured, see docs/ocr.md. Tags are pinned on
      # purpose: an untagged name silently follows :latest, which is how
      # production came to run an 8B model while the docs said 26B.
      # 1. image → text (purpose-built, ~2GB, ~4s; Tesseract is the backup)
      OLLAMA_OCR_MODEL: ${OLLAMA_OCR_MODEL:-glm-ocr:latest}
      # 2. text → JSON, runs on every document, chosen for latency + accuracy
      OLLAMA_PARSE_MODEL: ${OLLAMA_PARSE_MODEL:-gemma4:e2b}
      # 4. image → JSON, only for photos stage 1 could not read; must see
      OLLAMA_VISION_MODEL: ${OLLAMA_VISION_MODEL:-gemma4:e4b}
      # 3. second opinion — off by default; it never fired on the real corpus
      OLLAMA_VERIFY_MODEL: ${OLLAMA_VERIFY_MODEL:-}
      OCR_AUTO_APPLY_THRESHOLD: ${OCR_AUTO_APPLY_THRESHOLD:-0.7}
      OCR_TIMEOUT_MS: ${OCR_TIMEOUT_MS:-120000}
      # Ceiling on parallel documents. Limited by Ollama's memory rather than
      # CPU — see docs/ocr.md before raising it.
      OCR_CONCURRENCY: ${OCR_CONCURRENCY:-2}
      # In-App Purchases — numeric App Store app id (production notifications).
      IAP_APP_APPLE_ID: ${IAP_APP_APPLE_ID:-}
      VAPID_PUBLIC_KEY: ${VAPID_PUBLIC_KEY:-}
      VAPID_PRIVATE_KEY: ${VAPID_PRIVATE_KEY:-}
      VAPID_SUBJECT: ${VAPID_SUBJECT:-mailto:noreply@mototracker.app}
      # Native push (APNs) for the iOS app — off until all four below are set.
      # Web Push (VAPID) does not fire inside the iOS WebView; see
      # docs/app-store/native-push.md.
      APNS_KEY: ${APNS_KEY:-}
      APNS_KEY_ID: ${APNS_KEY_ID:-}
      APNS_TEAM_ID: ${APNS_TEAM_ID:-}
      APNS_BUNDLE_ID: ${APNS_BUNDLE_ID:-}
      APNS_PRODUCTION: ${APNS_PRODUCTION:-true}
      # MapKit JS (trip route maps) — .p8 with the MapKit JS service enabled.
      MAPKIT_KEY: ${MAPKIT_KEY:-}
      MAPKIT_KEY_ID: ${MAPKIT_KEY_ID:-}
      MAPKIT_TEAM_ID: ${MAPKIT_TEAM_ID:-}
      MAPKIT_MAPS_ID: ${MAPKIT_MAPS_ID:-}
      CRON_TIMEZONE: ${CRON_TIMEZONE:-Europe/Istanbul}
      CRON_HOUR: ${CRON_HOUR:-9}
      CRON_ENABLED: ${CRON_ENABLED:-true}
    volumes:
      # The image runs as the unprivileged `node` user (uid 1000). On Linux a
      # bind mount keeps the host directory's ownership, so run this once:
      #   mkdir -p data && sudo chown -R 1000:1000 data
      # Docker Desktop (macOS/Windows) remaps ownership for you.
      - ./data:/data
    # Bind to loopback only — public access goes through the tunnel
    # (Cloudflare in production; ngrok is legacy/opt-in).
    ports:
      - "127.0.0.1:8787:8787"
    # Allow the container to reach an Ollama running on the host
    # (Docker Desktop has this built-in; Linux needs host-gateway).
    extra_hosts:
      - "host.docker.internal:host-gateway"
    # morgan writes a line per request; without rotation the json-file driver
    # grows until the disk is full. 30 MB ceiling per container.
    logging: &default-logging
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    # Ceilings, not reservations — enough headroom for sharp + Tesseract on a
    # 10 MB upload. Override in .env if the box is smaller/larger.
    deploy:
      resources:
        limits:
          cpus: "${API_CPU_LIMIT:-2}"
          memory: ${API_MEM_LIMIT:-2g}

  # Mailpit — local SMTP catcher for dev. Catches all outgoing mail (magic links,
  # password resets) and shows it in a web inbox. No real email account needed.
  # Opt-in: `docker compose --profile mail up -d`.
  #   SMTP   → localhost:1025 (set SMTP_HOST=localhost for host `pnpm dev:api`,
  #            or SMTP_HOST=mailpit when the api runs in the compose network)
  #   Inbox  → http://localhost:8025
  mailpit:
    image: axllent/mailpit:latest
    container_name: mototracker-mailpit
    restart: unless-stopped
    profiles: ["mail"]
    logging: *default-logging
    ports:
      - "127.0.0.1:1025:1025"
      - "127.0.0.1:8025:8025"

  ollama:
    image: ollama/ollama:latest
    container_name: mototracker-ollama
    restart: unless-stopped
    # Opt-in: not started unless `docker compose --profile bundled-ollama up`.
    profiles: ["bundled-ollama"]
    logging: *default-logging
    ports:
      - "127.0.0.1:11434:11434"
    volumes:
      - ./ollama:/root/.ollama

  # Cloudflare Tunnel (production). Opt-in service: started only with
  # `docker compose --profile cloudflare up -d`. The public hostname + ingress
  # (→ http://api:8787) are configured in the Cloudflare Zero Trust dashboard;
  # this just runs the connector using the tunnel token. If you already run
  # cloudflared elsewhere (host service / separate container), leave this off.
  cloudflared:
    image: cloudflare/cloudflared:latest
    container_name: mototracker-cloudflared
    restart: unless-stopped
    profiles: ["cloudflare"]
    command: ["tunnel", "--no-autoupdate", "run", "--token", "${CLOUDFLARED_TOKEN}"]
    logging: *default-logging
    # Wait for the image's HEALTHCHECK (a real DB query) rather than just for
    # the container to exist — otherwise the tunnel goes public while the API
    # is still applying migrations and visitors get 502s.
    depends_on:
      api:
        condition: service_healthy

  # ngrok (legacy). Opt-in: `docker compose --profile ngrok up -d`.
  ngrok:
    image: ngrok/ngrok:latest
    container_name: mototracker-ngrok
    restart: unless-stopped
    profiles: ["ngrok"]
    environment:
      NGROK_AUTHTOKEN: ${NGROK_AUTHTOKEN}
    # Tunnel definition comes from ./ngrok.yml — bootstrap.sh writes this file
    # with `domain:` set when NGROK_DOMAIN is configured, omitted otherwise.
    command: ["start", "--all", "--log=stdout", "--config", "/etc/ngrok.yml"]
    volumes:
      - ./ngrok.yml:/etc/ngrok.yml:ro
    logging: *default-logging
    ports:
      # ngrok local web inspector — useful for debugging.
      - "127.0.0.1:4040:4040"
    depends_on:
      api:
        condition: service_healthy
